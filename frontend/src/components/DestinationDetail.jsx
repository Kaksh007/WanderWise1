// This component is used within DestinationPage
// Keeping it for potential reuse, but main logic is in DestinationPage

export default function DestinationDetail({ destination }) {
  return null // Implemented in DestinationPage
}

