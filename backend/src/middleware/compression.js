/**
 * Compression middleware
 * 
 * For production, install and use 'compression' package:
 * npm install compression
 * 
 * Then in server.js:
 * import compression from 'compression'
 * app.use(compression())
 * 
 * This file is a placeholder - compression is optional for MVP
 */

// Placeholder - compression can be added later if needed
export default (req, res, next) => {
  next()
}
